//5-2
test {
    int sub(int x, int y) {
        int c = x;
        int d = y;
        return c - d;
    }

    void prnt(int i) {
        int j = i;
        print i;
    }

    int res = sub(10, 5);
    void t = prnt(res);
    t = prnt(sub(2, 4));

    // Prints 5-2
}